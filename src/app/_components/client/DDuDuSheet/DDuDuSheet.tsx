import { BottomSheet } from "../BottomSheet";
import { DDuDuMenu } from "./components";

const DDuDuSheet = () => {
  return (
    <BottomSheet
      isShow
      defaultHeight={""}
    >
      <DDuDuMenu />
    </BottomSheet>
  );
};

export default DDuDuSheet;
