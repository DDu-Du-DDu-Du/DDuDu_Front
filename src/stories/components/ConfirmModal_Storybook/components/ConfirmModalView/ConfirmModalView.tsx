import { ConfirmModal } from "@/app/_components/client";
import { ConfirmModalProps } from "@/app/_components/client/ConfirmModal/ConfirmModal";

const ConfirmModalView = (props: ConfirmModalProps) => {
  return <ConfirmModal {...props} />;
};

export default ConfirmModalView;
