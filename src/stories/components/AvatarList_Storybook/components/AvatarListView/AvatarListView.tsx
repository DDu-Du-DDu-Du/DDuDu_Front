import AvatarList, { AvatarListProps } from "@/app/_components/client/AvatarList/AvatarList";

const AvatarListView = (props: AvatarListProps) => {
  return <AvatarList {...props} />;
};

export default AvatarListView;
