import { AlertModal } from "@/app/_components/client";
import { AlertModalProps } from "@/app/_components/client/AlertModal/AlertModal";

const AlertModalView = (props: AlertModalProps) => {
  return <AlertModal {...props} />;
};

export default AlertModalView;
